# Puerto en el que escucha el servidor (Cloud Run lo inyecta automáticamente como 8080)
PORT=8080

# CORS solo es necesario en desarrollo local (frontend en :5173, backend en :8080).
# En producción ambos comparten el mismo origen dentro del contenedor único.
CORS_ORIGIN=http://localhost:5173
