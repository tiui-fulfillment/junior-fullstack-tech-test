# Puerto en el que escucha el servidor (Cloud Run lo inyecta automáticamente)
PORT=3000

# Origen permitido para CORS (URL pública del frontend en Cloud Run)
# Ejemplo: https://frontend-abc123-uc.a.run.app
CORS_ORIGIN=*
